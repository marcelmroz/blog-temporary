<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <%- include('partials/header'); -%>

    <h1>Home</h1>
    <p><%= homeStartingContent_ %></p>
    <% if (posts.length > 0) { %>
        <% posts.forEach(function(post) { %>
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
            <a href="/posts/<%= post.title %>">See whole post</a>
        <% }); %>
    <% } else { %>
      <p>No posts yet.</p>
    <% } %>



    <%- include('partials/footer'); -%>
</body>
</html>